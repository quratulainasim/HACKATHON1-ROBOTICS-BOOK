{
  "name": "auth-server",
  "version": "1.0.0",
  "description": "Better Auth + PostgreSQL server",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "migrate": "ts-node src/migrate.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-auth": "^1.4.7",
    "dotenv": "^16.3.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
